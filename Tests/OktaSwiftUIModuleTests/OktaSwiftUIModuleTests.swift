import XCTest
@testable import OktaSwiftUIModule

final class OktaSwiftUIModuleTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(OktaSwiftUIModule().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
